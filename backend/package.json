{
  "name": "@keiras-journey/backend",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "Lambda handlers (Node 20 / TypeScript). modules/<m>/ per module; shared/ for frozen contracts.",
  "scripts": {
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@aws-sdk/client-dynamodb": "^3.700.0",
    "@aws-sdk/lib-dynamodb": "^3.700.0",
    "zod": "^3.23.8"
  }
}
